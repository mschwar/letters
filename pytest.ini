[pytest]
asyncio_default_fixture_loop_scope = function
# passlib 1.7.x imports `crypt`, which emits a Python 3.12 deprecation warning.
# Keep test output clean until upstream passlib drops that import.
filterwarnings =
    ignore:'crypt' is deprecated and slated for removal in Python 3.13:DeprecationWarning
